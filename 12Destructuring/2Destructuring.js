//HERE WE DESTRUCTURE THE DATA USING OBJECT

const myUser = {
    name : "BHARGAV",
    courseCount : 5,
    role : "ADMIN",
};

console.log(myUser.name);

//Or we can access it 

const { name , courseCount , role} = myUser;
console.log(role);




//it is just access it in different way 